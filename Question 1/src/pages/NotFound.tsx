import React from "react";
import {
  Box,
  Typography
} from "@mui/material";

const NotFound: React.FC = () => {
  return <Typography textAlign="center" variant="h2" sx={{ mt: 4 }}>404 Not Found</Typography>;
};

export default NotFound;
